apiVersion: v1
kind: ConfigMap
metadata:
  name: db-conf
  namespace: dev-review
data:
  host: mysql:3306  # host address of mysql server, we are using DNS of Service
  name: reviewdb # name of the database for application